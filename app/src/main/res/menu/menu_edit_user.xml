<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/groupTools"
        android:checkableBehavior="single">
        <item
            android:id="@+id/itemSaves"
            android:icon="@drawable/ic_save"
            android:title="@string/str_saves"
            app:showAsAction="ifRoom"/>

        <item
            android:id="@+id/itemClose"
            android:icon="@drawable/ic_close"
            android:title="@string/str_close"
            app:showAsAction="always"/>
    </group>

    <item
        android:id="@+id/itemMore"
        android:icon="@drawable/ic_more"
        android:title=""
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/itemEdit"
                android:icon="@drawable/ic_edit"
                android:title="@string/str_edit"
                app:showAsAction="withText"/>

            <item
                android:id="@+id/itemHelp"
                android:icon="@drawable/ic_info"
                android:title="@string/str_help"
                app:showAsAction="withText"/>
        </menu>
    </item>

</menu>